# Dependencies & build output
node_modules
.next
out
build
dist

# Generated artifacts
coverage
playwright-report
test-results
*.tsbuildinfo
next-env.d.ts

# Lockfiles (managed by the package manager)
pnpm-lock.yaml

# Data
data

# Vendored third-party sources — kept verbatim so they stay diffable against
# upstream. These carry their own formatting configs.
redis-js-main
my-agent

# Agent skill packs (vendored docs, synced from upstream)
.agents/skills

# Workflow files cannot be rewritten by the CI token (a GitHub App push
# without the `workflows` permission is rejected), so they are left as-is.
.github/workflows
