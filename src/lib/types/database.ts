// PLACEHOLDER. Run `npm run db:types` after applying the migrations in
// supabase/migrations/ to a real Supabase project — this generates the real
// typed schema from supabase/gen types and should overwrite this file.
//
// Until then, this loose type keeps the rest of the app compiling.
export type Database = Record<string, unknown>;
